# CLAUDE.md

@AGENTS.md

The context for this project lives in `AGENTS.md`, imported above, so that every
agent reads the same file rather than two copies that drift apart. Nothing
Claude-specific belongs here — put it in `AGENTS.md` where other tools will see
it too.

`ENGINEERING.md` is the long-form reasoning behind each decision. Read the relevant
section before changing that area.
